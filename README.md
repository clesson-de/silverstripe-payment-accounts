## Bankaccount extension for the Silverstripe contacts module

This module extends the contact module with the ability to link bank accounts to contacts.

### Installation ###

`composer require clesson-de/silverstripe-bankaccount`

Then run `/dev/build?flush=all`.
